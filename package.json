{
  "name": "ai-translator",
  "version": "1.0.0",
  "description": "Full-stack AI Translator with Groq API",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm install && npm start",
    "dev:frontend": "cd frontend && npm install && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node src/app.js"
  },
  "keywords": ["translator", "ai", "groq", "react", "nodejs"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
